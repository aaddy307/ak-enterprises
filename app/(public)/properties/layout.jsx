export const metadata = {
  title: "Properties",
  description: "Browse our curated collection of luxury properties in Ambernath.",
};

export default function PropertiesLayout({ children }) {
  return children;
}
